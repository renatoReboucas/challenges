<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.rr.challenges">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_winer2"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_winer2_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme" tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".LauncherActivity"
                  android:theme="@style/Theme.AppCompat.Light.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".ConversorActivity"
                android:theme="@style/Theme.AppCompat.Light.FullScreen">
        </activity>
        <activity
                android:name=".BissextoActivity"
                android:theme="@style/Theme.AppCompat.Light.FullScreen">
        </activity>
        <activity
                android:name=".ActivityDesafio"
                android:theme="@style/Theme.AppCompat.Light.FullScreen">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <meta-data android:name="preloaded_fonts" android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>